{
  "name": "todo-list",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install) "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "error-stack-parser": "^2.1.4"
  }
}
